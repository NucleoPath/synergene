# Copy to .env.local and fill in.
#
# The chat route calls Anthropic directly with the model chosen server-side,
# so this key is what makes sending a message work. See ADR-0003 for why the
# provider is not the client's choice.
ANTHROPIC_API_KEY=
